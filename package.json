{
  "name": "@local-brain/monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "oxlint apps packages",
    "lint:fix": "oxlint --fix apps packages",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "tauri": "pnpm --filter @local-brain/desktop tauri",
    "release:bump": "pnpm --filter @local-brain/desktop release:bump",
    "release:macos": "pnpm --filter @local-brain/desktop release:macos"
  },
  "devDependencies": {
    "oxlint": "^1.69.0",
    "turbo": "^2",
    "typescript": "~5.8.3"
  }
}
